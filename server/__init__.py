import os
from server.config import DATABASE_URL
from server.config import JWT_SECRET_KEY
from server.config import JWT_ACCESS_TOKEN_EXPIRES
from server.config import EMAIL_ID, EMAIL_PASSWORD

from server.config import S3_NAME, S3_REGION, S3_URL, S3_ACCESS, S3_SECRET